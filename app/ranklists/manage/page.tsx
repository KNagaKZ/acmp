import{redirect}from"next/navigation";import{getSession}from"@/lib/auth";import{RanklistManager}from"@/components/RanklistManager";export default async function Page(){const session=await getSession();if(!session)redirect("/login");return <RanklistManager isSuperAdmin={session.isSuperAdmin}/>}
